import 'package:flutter/material.dart';

class Shade{
  static const appbarColor=Color.fromARGB(255, 21, 50, 74);
  static const shadeWhite=Colors.white;
 static const LinearGradient bodyGradient = LinearGradient(
    colors: [
      Color(0xFF80CBC4), // Light teal
      Color(0xFF26A69A), // Medium teal
      Color(0xFF00796B), // Dark teal
    ],
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
  );
static const textcolor=Colors.black12;

}
